# Lightweight test-only dependencies for CI.
# Avoids installing llama-index, langchain, pymupdf etc. just to run unit tests.
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.0,<3
pytest>=8,<9
httpx>=0.28,<1
ruff>=0.9,<1
